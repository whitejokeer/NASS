import 'package:flutter/material.dart';

class SharedDataPage extends StatelessWidget {
  final String sharedID;

  const SharedDataPage({Key key, this.sharedID}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
